<nav class="navbar d-flex flex-column vh-100 d-flex bg-light border-end border-danger p-4">
  <div class="d-flex flex-column justify-content-between h-100">
    <%= logo('h1') %>

    <%= link_to(edit_user_registration_path, class: 'nav-link d-flex flex-column gap-2 align-items-center') do %>
      <i class="bi bi-gear-fill text-body-tertiary fs-4"></i>
      <span class="fw-bold text-body-tertiary">Settings</span>
    <% end %>
  </div>
</nav>
