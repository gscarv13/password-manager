<h1> Entries </h1>

<div class="row">
  <div class="card">
    <div class="card-body">
      <% if @entries.any?  %>
        <%= render(@entries) %>
      <% else %>
        No Entries yet. <%= link_to("Add new entry", new_entry_path) %>
      <% end %>
    </div>
  </div>
</div>
