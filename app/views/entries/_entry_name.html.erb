  <div class="d-flex align-items-center">
    <div class="entry-favicon">
      <%= image_tag(
        "https://icons.duckduckgo.com/ip3/#{URI.parse(entry.url).host}.ico",
          alt: entry.name
      )%>
    </div>
    <%= yield %>
  </div>
