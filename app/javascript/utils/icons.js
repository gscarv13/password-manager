export function buildIcon(iconName) {
  const icon = document.createElement('i');
  icon.className = `bi ${iconName}`;

  return icon
}

export const checkIcon = "bi-clipboard2-check";
export const clipBoardIcon = "bi-clipboard-plus";

